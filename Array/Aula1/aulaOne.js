const note = [1, 2, 3, 4, 5, 6, 7, 8]

console.log(note)
console.log(note[1]) // imprir o index do array
console.log(note[000]) // resultado será undefind

note[2] = 0 // atribuindo um novo valor a patir do index
console.log(note)